flows:
  master:
    - uname -a > master.txt
    - (on osx, name osx-node) uname -a > osx.txt
    - (on linux, name linux-node) uname -a > linux.txt
    - call: build

  build:
    - (on linux):
      - uname -a
      - ls -la
    - (on osx):
      - uname -a
      - ls -la
